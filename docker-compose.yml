services:
  db:
    image: postgres:15-alpine
    container_name: ai_postgres
    restart: always
    environment:
      POSTGRES_USER: zosi_admin
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ai_platform_db
    ports:
      - "127.0.0.1:5432:5432"   # μόνο τοπικά (host) — ΟΧΙ εκτεθειμένο στο δίκτυο
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: ./backend
    container_name: ai_backend
    restart: always
    ports:
      - "127.0.0.1:8000:8000"   # μόνο τοπικά/proxy — ΟΧΙ δημόσιο
    volumes:
      - ./backend:/app
      - chromadb_data:/app/vector_db
      - huggingface_cache:/home/appuser/.cache/huggingface
    environment:
      - DATABASE_URL=postgresql://zosi_admin:${POSTGRES_PASSWORD}@db:5432/ai_platform_db
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - GEMINI_MODEL=${GEMINI_MODEL:-gemini-2.5-flash}
      - SECRET_KEY=${SECRET_KEY}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-http://localhost:8501}
    depends_on:
      - db
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/health')\" || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 300s

  frontend:
    build: ./frontend
    container_name: ai_frontend
    restart: always
    ports:
      - "8501:8501"   # το UI (τοπικά). Στο deploy: πίσω από reverse proxy + HTTPS
    volumes:
      - ./frontend:/app
    environment:
      - BACKEND_URL=http://backend:8000
    depends_on:
      backend:
        condition: service_healthy

volumes:
  postgres_data:
  chromadb_data:
  huggingface_cache: