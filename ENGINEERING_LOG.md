# Z-AI Platform v2 — RAG · Engineering Log

Τεχνικό ημερολόγιο αποφάσεων για ένα local-first RAG σύστημα πάνω σε επιστημονικά PDF.
Κάθε απόφαση εδώ συνοδεύεται από **μέτρηση στα δικά μας δεδομένα** — τι αλλάζει, γιατί
έγινε η μέτρηση, και τι θα σήμαινε αν έβγαινε αλλιώς. Οι ισχυρισμοί από papers ή από
τρίτα benchmarks δεν γίνονται δεκτοί χωρίς επαλήθευση στο δικό μας corpus· έχουν
διαψευστεί δώδεκα φορές (BGE sparse, jina reranker, ONNX INT8, torch INT8, query
decomposition, «μεγαλύτερο reranker = καλύτερο», «τα μικρά chunks είναι καθολικά
καλύτερα», «στους 8+ χρήστες το σύστημα καταρρέει» — όλα ανατράπηκαν από τη μέτρηση).

Δύο αρχές που διατρέχουν όλο το log:

- **Μια απόρριψη ισχύει μόνο για το σετ πάνω στο οποίο έγινε.** Το `bge-v2-m3`
  απορρίφθηκε αρχικά σε ένα σετ με survivorship bias· όταν το σετ αποδείχθηκε
  προβληματικό, η απόρριψη έπρεπε να ξαναελεγχθεί — και το ενδιάμεσο μέγεθος reranker
  (L-12) δεν είχε δοκιμαστεί ποτέ. Εκεί ήταν όλο το κέρδος.
- **Όταν ένα σετ δίνει τέλειο σκορ, ύποπτο είναι το σετ.** Το relevance gate έβγαζε
  61/61 επειδή είχε αφαιρεθεί η ερώτηση που το χαλούσε. Όταν ένα αποτέλεσμα αντιφάσκει
  με προηγούμενη πιο αξιόπιστη μέτρηση, ύποπτο είναι το benchmark — όχι το σύστημα.

---

## Το σύστημα

Local-first RAG για επιστημονικά PDF, δίγλωσσο EL/EN.

**Stack:** FastAPI 0.99.1 + Pydantic V1 (pinned) · Streamlit · ChromaDB 0.4.6 ·
PostgreSQL · Gemini 2.5 Flash · Docker Compose. **CPU only** (Ryzen 7 5700X, 32GB RAM,
χωρίς GPU· ο container βλέπει 8 πυρήνες / 11.7GB).

**Corpus:** 7 open-access papers (cloud/serverless), 122 σελίδες, **418 chunks**.
Εξαγωγή PDF: pymupdf + NFKC + de-hyphenation. chunk_size=1500 / overlap=300, ανά σελίδα.

### Pipeline (`backend/ai_core.py` → `search_documents`)

1. **translate-then-retrieve** (Gemini, μόνιμο cache σε JSON) — domain-aware prompt
2. dense **bge-m3** (1024-d, cosine) — exact brute-force, **όχι HNSW** · `DENSE_CANDIDATES=30`
3. **BM25** με ελληνικό tokenizer (30 υποψήφιοι)
4. **RRF** k=60 με ντετερμινιστικό tie-break στο chunk id → top-`RERANK_CANDIDATES=15`
5. cross-encoder **ms-marco-MiniLM-L-12-v2** (33M, αγγλικό — το pipeline μεταφράζει πριν) · `RERANK_BATCH_SIZE=4`
6. **relevance gate** `MIN_RERANK_SCORE=-2.6` (ωμά logits, όχι sigmoid)
6β. **corrective retry** — μόνο αν το βήμα 6 έκοψε: rewrite με Gemini → πλήρες 2ο pass
   (2-5) → επιεικέστερο `CORRECTIVE_MIN_SCORE=-3.8`. Μηδέν κόστος στο happy path.
   Το κατώφλι είναι 4.2 logits πιο κάτω από το gate επειδή η **κλίμακα** του 2ου pass
   είναι άλλη: το ms-marco-MiniLM είναι εκπαιδευμένο σε ερωτήσεις, ενώ ο corrective
   agent παράγει ονοματικές φράσεις — όλη η κατανομή του 2ου pass κάθεται χαμηλότερα.
7. `_expand_to_pages(sorted_final[:EXPAND_INPUT=12], MAX_PAGES=8)`
8. Gemini streaming μέσω REST (`gemini_rest.py`) + metrics packet προς το UI
9. **inline citations**: το context φέρει ετικέτες `[S{i}: {file}, Page: {page}]` και ο
   system prompt ζητάει σκέτο `[S3]` στο τέλος κάθε πρότασης. Η ετικέτα ταξιδεύει και
   στο `sources` packet· το UI δεν ξαναριθμεί.

**Caches** (per-process, με locks): BM25 index, dense matrix, sparse weights,
translations, query embeddings. Κλειδί ακύρωσης: `_corpus_signature()` = (τοπικός
μετρητής, mtime του chroma.sqlite3) — ορατό μεταξύ διεργασιών.

---

## Μεθοδολογία αξιολόγησης

Η αξιοπιστία των αριθμών στηρίζεται σε τέσσερα εργαλεία, όχι σε ένα:

- **Τέσσερα golden sets, σκόπιμα ξεχωριστά:**
  - `golden_set_50` — 45 in-corpus + 5 out_of_corpus· το σταθερό baseline. Οι
    out_of_corpus ερωτήσεις είναι αυτές που κάνουν ορατή την άμυνα κατά της ψευδαίσθησης.
  - `golden_multihop_new` — 11 cross-document multi_hop· το πιο έντιμα μετρημένο σετ
    (πάτωμα τύχης 22.6%, κανένα αδύναμο keyword).
  - `golden_hard_paraphrase` — 16 σκόπιμα κακοδιατυπωμένες παραφράσεις *υπαρχουσών*
    ερωτήσεων. Stress test, όχι baseline: ίδια keywords με τη γονική ερώτηση, αλλάζει
    μόνο η διατύπωση — αν το gate κόψει, η κοπή είναι αποδεδειγμένα λάθος.
  - `golden_conversations` — 12 πολύγυρες συνομιλίες (10 in-corpus + 2 leak tests).
- **LLM-judge** (Gemini 2.5 Flash) — accuracy / completeness / relevance / faithfulness.
- **RAGAS** — ανεξάρτητος δεύτερος κριτής, για έλεγχο self-preference bias.
- **Bootstrap CI** (ζευγαρωτό, 10.000 resamples) — απαντά «είναι πραγματική η διαφορά
  σε αυτό το n;». Το πάτωμα θορύβου μετρήθηκε **±0.04 σε n=45**, τετραπλάσιο του
  εμπειρικού ±0.01. Συνέπεια: οι περισσότερες αποφάσεις πάρθηκαν σε περιοχή όπου η
  στατιστική δεν αποφαίνεται — εγγενές όριο 122 σελίδων / 45 ερωτήσεων.
- **Random floor** (`random_coverage_baseline.py`, υπεργεωμετρική) — κάθε ποσοστό
  coverage διαβάζεται ως προς το δικό του πάτωμα τύχης. Κανόνας: πριν πιστέψεις
  οποιοδήποτε coverage, τρέξε το πάτωμα του.

---

## Τρέχοντα αποτελέσματα

### Retrieval (ντετερμινιστικά, επαναλήψιμα, MiniLM-L-12)
```
in-corpus (n=45)        MRR 0.793 · nDCG 0.807 · coverage 98.5%
out_of_corpus (n=5)     0.000 — και τα 5 κόβονται σωστά από το gate
ανά κατηγορία: direct_fact 0.869 · enumeration 0.751 · reasoning 0.831 · multi_hop 0.541
τυχαίο baseline in-corpus: 0.149
```
Το multi_hop 0.541 είναι ~15× πάνω από την τύχη του· κανονικοποιημένο, σκοράρει
*καλύτερα* από τον μέσο όρο. Το πραγματικό του πρόβλημα φαίνεται στον judge (σύνθεση
από δύο έγγραφα), όχι στο MRR.

### Η θετική σκάλα — τι προσθέτει κάθε στάδιο (`ablation_ladder.py`, δωρεάν)
```
στάδιο           MRR      ooc σωστά σιωπηλά
dense μόνο       0.731         0/5
+ BM25 / RRF     0.774         0/5      (+0.043)
+ cross-encoder  0.793         0/5      (+0.019)
+ relevance gate 0.793         5/5      (+0.000)
```
Το gate δεν βελτιώνει καμία μετρική κατάταξης — και είναι το σημαντικότερο στάδιο. Όλη
η υβριδική μηχανική δίνει +8.5% MRR· το gate δίνει το 100% της αξιοπιστίας: χωρίς αυτό
το σύστημα απαντά πάντα, ακόμα και στις 5 ερωτήσεις που το corpus δεν μπορεί να
απαντήσει. Είναι ορατό μόνο επειδή το golden set έχει ερωτήσεις χωρίς απάντηση.

### Relevance gate — κατανομή (`measure_gate_margin.py`, n=61)
```
                 in-corpus (56)                    out_of_corpus (5)     κενό
L-12   min -2.08 · p10 0.81 · διάμ 4.62 · max 9.27      max -3.12        1.04
```
Ολόκληρο το εύρος `[-3.12, -2.08]` δίνει 61/61· το **-2.6** είναι το μέσο (0.52
περιθώριο και προς τις δύο πλευρές — μεγιστοποιεί το χειρότερο περιθώριο).

### Answer quality (LLM-judge)
```
χωρίς multi_hop (n=46)  accuracy 5.00 · completeness 4.98 · relevance 5.00 · faithfulness 5.00
όλες (n=50)             4.94 / 4.90 / 4.96 / 4.94
```
**Ο κριτής είναι μη ντετερμινιστικός:** με ίδιο σύστημα σε δύο τρεξίματα, 2/9 ερωτήσεις
του υποσυνόλου ρίσκου γυρίζουν κατά μία μονάδα. Συνέπεια: η διαφορά 48/50 vs 46/50 δεν
αποδεικνύει τίποτα, και δύο judge runs δεν συγκρίνονται σαν να είναι το ίδιο πείραμα.
Με 48/50 ερωτήσεις ήδη 5/5/5/5, ένα judge run μπορεί να δείξει μόνο μη-υποβάθμιση, όχι
βελτίωση.

### RAGAS cross-validation (n=45, ανεξάρτητος κριτής)
```
faithfulness 0.9920 · context_recall 1.0000 · answer_relevancy 0.8552 · context_precision 0.7826
```
Δύο ανεξάρτητοι κριτές συμφωνούν στο faithfulness — self-preference bias δεν υπήρξε.
Το `context_precision 0.783` είναι το τίμημα του page-level expansion (recall
0.944→1.000, precision 0.800→0.783): συνειδητή ανταλλαγή, με μηδέν ψευδαισθήσεις.

### Latency
```
warm retrieval  846 ms  (rerank 784 = 92.8%, expand 53.2, authz_get 5.3, bm25 1.4, dense 0.5)
end-to-end      2.8-4.3 s · prompt ~9.800 tokens
```
Το retrieval είναι ~20-29% του χρόνου· ο μοχλός παραμένει η γέννηση. Το rerank κυριαρχεί
(92.8%), οπότε κάθε βελτιστοποίηση εκτός rerank έχει ταβάνι ~7% (νόμος του Amdahl).

### Concurrency (`concurrency_benchmark.py`, ως 32 χρήστες)
```
χρήστες      p50       p95     throughput   p50 vs 1 χρήστη
   1        0.86s     0.88s     1.18 req/s      1.00x
   4        2.82s     3.16s     1.38 req/s      3.28x   <-- κορεσμός
   8        8.66s     9.39s     0.90 req/s     10.07x
  16       13.84s    16.94s     0.98 req/s     16.09x
  32       28.08s    32.04s     1.00 req/s     32.65x
```
**Πλατό, όχι κατάρρευση.** Το throughput σταθεροποιείται ~1.0 req/s· το p50 μεγαλώνει
σχεδόν γραμμικά (νόμος του Little, `W = L/λ`). Είναι η υπογραφή μιας ουράς που δουλεύει,
όχι συστήματος που κάνει thrashing. Το benchmark μετράει μόνο το CPU-bound retrieval —
στην παραγωγή η γέννηση (2-4 s I/O-bound) απλώνει τους χρήστες, άρα τα νούμερα είναι
χειρότερης περίπτωσης. Bottleneck: η CPU του reranker.

### Scaling (synthetic, `scaling_benchmark.py`)
| chunks | dense exact | HNSW | BM25 query | BM25 build | RRF |
|---|---|---|---|---|---|
| 418 | 0.04 ms | 0.06 ms | 0.2 ms | 0.1 s | 0.06 ms |
| 50.000 | 9.2 ms | 0.85 ms | **61 ms** | 7.8 s | 3.9 ms |
| 200.000 | 46 ms | 0.82 ms | — | — | 22 ms |

Το BM25 σπάει πρώτο, όχι το vector search. Η πρώτη αναβάθμιση κλίμακας θα ήταν
Tantivy/Postgres FTS, όχι Pinecone. Το exact search στέκει πέρα από 200k chunks.

### Κόστος (`measure_cost.py`, μηδέν κλήσεις γέννησης)
```
ανά ερώτηση    context 7.9 σελ / 40.739 χαρ -> 9.693 tokens in · 1.112 out · 0.00569 $
ανά 1.000      Gemini in 2.91 $ · out 2.78 $ · embeddings/rerank/BM25 0.00 $  =  5.69 $
μήνας          VPS 5 € + Gemini 5.27 € = 10.27 €
```
Δύο ευρήματα: (1) η έξοδος κοστίζει **8.3× ανά token** — κάθε απόφαση για το μέγεθος
context τραβούσε τον φθηνό μοχλό. (2) Το `THINKING_BUDGET=512` είναι το 21% του
λογαριασμού, αλλά είναι τιμή, όχι σπατάλη: το 0 έριξε μία faithfulness 5.0→2.0.
Embedding/reranking/BM25 = 0 $ ανά κλήση επειδή τρέχουν σε CPU — ένα GPU demo δεν μπορεί
να το πει αυτό.

---

## Αποφάσεις που κρατήθηκαν (με μέτρηση)

| Αλλαγή | Αποτέλεσμα |
|---|---|
| pypdf → pymupdf + NFKC | σπασμένα tokens 3.7%→2.5%, MRR +0.026 |
| gate 0.15 → 0.05 → −2.0 (MiniLM) | MRR +0.018 |
| bge-reranker-v2-m3 → ms-marco-MiniLM-L-6 | 15.048 ms → 693 ms (21.7×), accuracy 4.96→5.00 |
| HNSW → exact brute-force | ταυτόσημο top-30, ντετερμινιστικό — και ξεκλείδωσε το multi-worker |
| query embedding cache | 1190 ms → 0.3 ms στο warm path |
| ντετερμινιστικό RRF | έκανε κάθε επόμενη μέτρηση αξιόπιστη |
| `torch.set_num_threads(8)` | rerank 667→479 ms (1.39×)· το PyTorch διάλεγε 4 λόγω WSL2 heuristic |
| `RERANK_BATCH_SIZE=4` | 479→434 ms (1.15×), Pearson ρ=1.0000, 0 top-1 flips |
| REST γέννηση + `THINKING_BUDGET=512` | TTFT 3.90→2.78 s· ~1000 κρυφά thinking tokens σβήστηκαν |
| `_corpus_signature` με mtime | διέγραψε το #1 known limitation (multi-worker) |
| domain-aware translation prompt | «μηχανήματα»→machinery· δύο ερωτήσεις +7.74 και +4.14 logits· στο κύριο σετ ουδέτερο, 30 αγγλικές max \|Δ\|=0.000 |
| corrective retrieval agent | 4 ερωτήσεις από σιωπή → σωστή απάντηση, 0 ψευδαισθήσεις, out_of_corpus 5/5, κύριο σετ 61/61 ανέπαφο |
| `CORRECTIVE_MIN_SCORE` +0.4 → −3.8 | hard set 12/16 → 13/16· το κατώφλι ήταν βαθμονομημένο σε λάθος κλίμακα (ερωτήσεις vs ονοματικές φράσεις) |
| `metrics.py` + `/metrics` (Prometheus text, 0 εξαρτήσεις) | νέα ορατότητα: gate_block_rate, corrective_success_rate, tokens, latency ανά φάση |
| MiniLM-L-6 → MiniLM-L-12 (22M→33M) + gate −2.0→−2.6 + corrective +1.0→+0.4 | hard set 10/16→12/16· κενό gate 0.89→1.04· σωστό chunk στη θέση 1 36→40/56· corrective ψευδαισθήσεις 2→0. Κόστος: +275 ms (+9% e2e). Coverage 98.5% ταυτόσημο ανά ερώτηση |
| `UNIQUE(user_id, file_hash)` στο `Document` | το ίδιο PDF δεύτερη φορά διπλασίαζε τα chunks του· το RRF τα έβλεπε ως δύο ανεξάρτητες ψήφους. Ο περιορισμός το κόβει στη ρίζα |
| `created_at` σε `Conversation` | χωρίς αυτό, καμία χρονική διάσταση (retention, FinOps ανά περίοδο)· migration χωρίς default (παλιές γραμμές → NULL = «άγνωστο») |
| inline citations, μορφή `bare` (`[S3]`) | επινοημένη πηγή γίνεται μη αναπαραστάσιμη εξ ορισμού: 118 παραπομπές, 0 επινοημένες, θεμελίωση 0.867 έναντι τύχης 0.509· κόστος +1% tokens εισόδου |
| `_has_dangling_referent` | ο μόνος έλεγχος που κοιτάει την ερώτηση *πριν* την ανάκτηση· πιάνει ερωτήσεις με ασυμπλήρωτο αναφορικό· 4 σημαίες, 0 ψευδώς θετικές σε 105 ερωτήσεις |
| rate limiting σε PostgreSQL (`rate_limit.py`) | fixed window με atomic `INSERT .. ON CONFLICT DO UPDATE .. RETURNING`· μοιράζεται μεταξύ διεργασιών, επιβιώνει restart, μηδέν νέα containers |

---

## Πειράματα που απορρίφθηκαν (με μέτρηση)

Αυτός ο πίνακας είναι το πιο χρήσιμο κομμάτι του log: κάθε γραμμή είναι μια ιδέα που
ακουγόταν λογική και ανατράπηκε από τη μέτρηση. Η αξία δεν είναι η ιδέα — είναι ο λόγος
που έπεσε.

| Τι | Γιατί απορρίφθηκε |
|---|---|
| sum-of-top-K page scoring | MRR +0.0006· μεροληψία υπέρ πυκνών σελίδων |
| per-source page cap | multi_hop αμετάβλητο (0.364→0.364) |
| BGE-M3 native sparse (3ο σκέλος RRF) | 0.799→0.779 |
| EXPAND_INPUT 12→15 | ταυτόσημο σε 50/50 |
| MAX_PAGES 5 / 6 / 12 | coverage −2.2pp, nDCG ταυτόσημο |
| ONNX fp32 / INT8 | fp32 πιο αργό, int8 ίδιο |
| torch dynamic INT8 (fbgemm) | 1.37× ταχύτερο αλλά σπάει το gate: in-min πέφτει κάτω από το κατώφλι. ρ=0.9970 και παρ' όλα αυτά αποτυγχάνει — η συσχέτιση είναι άχρηστη μετρική για reranker |
| `THINKING_BUDGET=0` | 2.73× ταχύτερο TTFT αλλά faithfulness 5.0→2.0· και οι 3 υποβαθμίσεις σε multi_hop |
| `bge-reranker-base` (278M) | χειρότερο και από το 22M: hard 3/16· βαθμολογεί ψηλά τα out_of_corpus, οπότε το κατώφλι σκοτώνει και τα σωστά. Η διαχωρισιμότητα δεν κλιμακώνεται με το μέγεθος |
| `bge-reranker-v2-m3` (568M) | +1 πραγματική, +1 ψευδαίσθηση = καθαρό μηδέν, για 17× παραμέτρους και ~15 s/ερώτηση |
| jina-reranker-v2 | το πλεονέκτημά του είναι Flash Attention 2 = GPU-only |
| Postgres/pgvector migration | μηδενική βελτίωση· το scaling benchmark έδειξε ότι το BM25 σπάει πρώτο, όχι το vector store |
| contextual compression | δεν λύνει μετρημένο πρόβλημα (faithfulness 0.992, recall 1.000) |
| query decomposition για multi_hop | 3 στρατηγικές, καλύτερη +2.3% = 1 keyword σε 45· κόστος +1.5-2.3 s + 1 κλήση Gemini σε κάθε ερώτηση. Τα multi_hop βγάζουν ήδη judge 5.00 |
| query enrichment με ορολογία (3 εκδοχές) | ο εμπλουτισμός βοηθά μόνο όταν ο reranker βλέπει τις πρόσθετες λέξεις — και τότε ακριβώς παραπλανάται το gate (διαρροή out_of_corpus). Αρχιτεκτονικό όριο, όχι bug |
| πιο ευαίσθητο trigger για τον corrective agent | η βαθμολογία δεν μετράει ορθότητα: ερώτηση με μηδέν σωστό υλικό σκοράρει *ψηλότερα* από ερώτηση με το σωστό στη θέση 1. Καμία τιμή κατωφλίου δεν διαχωρίζει «δεν βρήκα» από «βρήκα λάθος» |
| πολλαπλές προσπάθειες corrective (N=3) | temperature 0.1 → 7/9 αναδιατυπώσεις ίδιες λέξη προς λέξη· μηδέν διασπορά να εκμεταλλευτείς |
| ετυμηγορία θεμελίωσης από τον generator | 1 κερδισμένη έναντι 4 λανθασμένων αρνήσεων. Εύρημα-μπόνους: η ετυμηγορία είναι γλωσσοεξαρτώμενη (ελληνικά 6 ΝΑΙ/14 ΟΧΙ vs αγγλικά 34/2) — κάθε LLM-έλεγχος πρέπει να τρέχει στη γλώσσα του *σώματος*, όχι του χρήστη |
| RRF με εγγυημένες θέσεις ανά σκέλος | υποψήφιοι άλλαξαν 75/77, σελίδες 4/77, coverage αμετάβλητο. Ο reranker απορροφά την αναδιάταξη, όχι την αφαίρεση |
| τρίτο στάδιο μετά τον corrective (κριτής / quote-first / RBO) | τρεις ανεξάρτητοι μηχανισμοί συγκλίνουν στο ίδιο 3/1/0 επειδή και οι τρεις κρίνουν το *κείμενο* — το όριο δεν είναι ο μηχανισμός. Ένας επαληθευτής που βλέπει μόνο τις σελίδες δεν εντοπίζει ασυμπλήρωτο αναφορικό στην ερώτηση |
| cascade reranking (L-6 φιλτράρει, L-12 κρίνει) | αριθμητικά: 434+282 ms > 706 ms. Το cascade θέλει φθηνό 1ο στάδιο 10-100×· εδώ ο λόγος είναι 1.63× |
| `chunk_size` 750 / 1000 | το πρόσημο αντιστρέφεται ανάμεσα στα σετ: στο κύριο 750 δίνει *καλύτερο* MRR, στο hard *χειρότερο* (τα multi_hop καταρρέουν). Το κύριο σετ είναι στο ταβάνι κάλυψης και δεν μπορεί να δείξει την απώλεια |
| `chunk_size` > 1500 | ο CrossEncoder περικόπτει σιωπηλά στα 512 tokens (~2.230 χαρ)· στο 2000 θα κόβονταν ~25% των chunks |
| `RERANK_CANDIDATES` 15 → 12 / 10 | φαινομενικά δωρεάν (ίδιο best-logit/gate/kw@1) αλλά αλλάζει τις σελίδες που φτάνουν στο Gemini σε 42-52/56 ερωτήσεις — μεγαλύτερη αλλαγή prompt από την ίδια την αλλαγή μοντέλου |
| backpressure (semaphore + 429) | το σύστημα ήδη σειριοποιείται αόρατα (ThreadPoolExecutor + torch threads)· ένα 429 δεν θα έδινε ταχύτερες απαντήσεις, θα έδινε λιγότερες. Δεν είναι κατάρρευση, είναι πλατό |
| μορφές παραπομπής `full` / `label` | τα ονόματα PDF τοκενίζονται σε 1.13 χαρ/token (vs 3.57 του κειμένου)· 83% της αύξησης tokens ήταν οι ίδιες οι συμβολοσειρές. Η `label` έβγαλε 10 λάθος σελίδες σε 97 (το μοντέλο αντέγραφε τον τυπωμένο αριθμό σελίδας). Ό,τι δεν ζητάμε από το μοντέλο δεν μπορεί να βγει λάθος |
| disambiguation-by-retrieval | 0 κερδισμένες, 5 διαρροές out_of_corpus: το prompt υποχρεώνει να ονομαστεί paper του corpus, άρα το ερώτημα κληρονομεί το λεξιλόγιο του corpus και ο reranker βαθμολογεί τον τίτλο, όχι την ερώτηση |
| λακωνικό corrective prompt | queries 2-3 λέξεων, ελάχιστο σήμα· και το κρίσιμο: η υψηλότερη βαθμολογία έγινε ψευδαίσθηση, οπότε κανένα κατώφλι δεν τη φιλτράρει. Ένα prompt μπορεί να χαλάσει τη δυνατότητα φιλτραρίσματος, όχι μόνο την ποιότητα |

---

## Κεντρικά ευρήματα

**1. Το retrieval MRR είναι αποσυνδεδεμένο από την ποιότητα απάντησης σε αυτό το εύρος.**
- reasoning MRR 0.823 → answers 5.00/5.00/5.00/5.00
- q036 MRR 1.000 → accuracy 4, completeness 4
- reranker swap: MRR −0.052, accuracy 4.96→5.00

Με coverage 97%+, το σωστό υλικό φτάνει ήδη στο μοντέλο· το «lost in the middle» δεν
εμφανίζεται στα ~9.800 tokens με Gemini 2.5 Flash. **Οι αλλαγές κρίνονται με coverage +
judge run, όχι με MRR.** Και το αντίστροφο: καμία μετρική ενδιάμεσου σταδίου δεν
προβλέπει το τελικό prompt — το L-12 έδωσε ταυτόσημο coverage σε 45/45 και όμως 35/56
ερωτήσεις πήραν άλλες σελίδες, επειδή ο reranker αναδιατάσσει.

**2. Το golden set κρύβει τις αποτυχίες του δημιουργού του.** Το gate έβγαζε 61/61
τέλειο — και ήταν ψευδές: η ερώτηση που το χαλούσε είχε αφαιρεθεί (survivorship bias).
16 σκόπιμα κακοδιατυπωμένες παραφράσεις υπαρχουσών ερωτήσεων αποκάλυψαν 11/16 λάθος
κοπές. Η αντιμετώπιση ήταν ένα ξεχωριστό stress-test σετ, όχι πιο επιεικές κατώφλι.

**3. Ο reranker είναι lexical-driven — σε δύο από τα τρία στάδια ανάκτησης.** Το ίδιο
ερώτημα χωρίς την ορολογία των papers πέφτει έως 12 logits, με το σωστό chunk στη θέση 1.
Το tuning κατωφλίου είναι νεκρός δρόμος όταν οι κατανομές επικαλύπτονται — η αλλαγή
πρέπει να γίνει στο query. Και το BM25 απαιτεί λεξική συμφωνία: ένα μοναχικό dense hit
στη θέση 15 κόβεται από το RRF πριν καν το δει ο reranker.

**4. Η αξιοπιστία στηρίζεται σε δύο ανεξάρτητα στρώματα, όχι σε ένα κατώφλι.** Όταν
δοκιμάστηκε το χαμηλότερο δυνατό `CORRECTIVE_MIN_SCORE`, μία ερώτηση με κάλυψη 0%
πέρασε το κατώφλι — και το system prompt (2η γραμμή άμυνας) την απέρριψε και τις 5
φορές. Η άμυνα κατά της ψευδαίσθησης δεν είναι το gate· είναι gate *και* prompt.

---

## Γνωστοί περιορισμοί

- **`CORRECTIVE_MIN_SCORE=-3.8` βαθμονομημένο σε n=6, χωρίς validation set.** Το πιο
  εύθραυστο σημείο της αλυσίδας, αλλά κάθεται στο μέσο ενός καθαρού διαστήματος 1.31
  logits. Αν μεγαλώσει το golden set, ξαναμετριέται.
- **Το «σώθηκαν N» του corrective δεν είναι ντετερμινιστικό:** το rewrite το γράφει το
  Gemini χωρίς σταθερό seed. Μετρήθηκε μικρό (μέσο εύρος 0.43 logits έναντι κενού gate
  1.04), αλλά υπαρκτό — δύο εκτελέσεις του `verify_corrective` δεν συγκρίνονται σαν ίδιο
  πείραμα.
- **h002 — αποτυχία με ακριβή μηχανισμό:** η σελίδα-στόχος βρίσκεται από το dense (θέση
  15) αλλά απουσιάζει από το BM25, οπότε το RRF την κόβει· το gate δεν ενεργοποιείται
  (top-1 = +1.27), άρα ο corrective δεν τρέχει ποτέ. Η άμυνα πιάνει το «δεν ξέρω», όχι
  το «νομίζω ότι ξέρω». Δεν διορθώνεται φθηνά — γενικεύει το «ο reranker είναι
  lexical-driven».
- **3/16 hard άλυτες, όχι για τον ίδιο λόγο:** μία κλειστή από αριθμητική (0.019 logits
  από out_of_corpus, κανένα κατώφλι δεν τη σώζει χωρίς διαρροή), δύο με ασυμπλήρωτο
  αναφορικό («η συγκεκριμένη υλοποίηση», «το άλλο paper») — τίποτα κατάντη ή ανάντη της
  ερώτησης δεν τις λύνει· η σιωπή είναι η σωστή απάντηση. Στην παραγωγή λύνονται από το
  conversational rewriting (90% κάλυψη vs 41.2% τύχης)· το hard set τις θέτει σκόπιμα
  ξεκάρφωτες, χωρίς ιστορικό.
- `google.generativeai` deprecated· έχει μείνει σε ένα σημείο που τρέχει μόνο εκτός
  παραγωγής. Η μετανάστευση σε `google.genai` απαιτεί pydantic≥2.12 έναντι του
  καρφωμένου 1.10 — τεκμηριωμένο χρέος.
- Ταυτόχρονο διπλό upload: το `UNIQUE(user_id, file_hash)` πιάνει τη βάση, αλλά δύο
  αιτήματα που ξεκινούν μαζί κάνουν και τα δύο ingest πριν σκάσει το δεύτερο commit →
  ορφανά chunks. Χρειάζεται `try/except IntegrityError` γύρω από το ingest.
- Το mtime-based cache invalidation δουλεύει σε ένα filesystem· πολλαπλά μηχανήματα
  θέλουν Redis.
- `--workers 1` από χωρητικότητα (2.4GB μοντέλα/worker + κορεσμός CPU στους 4 χρήστες),
  όχι από ορθότητα.

---

*Το ημερολόγιο καταγράφει μετρήσεις πάνω σε 122 σελίδες / 45 ερωτήσεις. Πολλές αποφάσεις
πάρθηκαν σε περιοχή που η στατιστική δεν αποφαίνεται (πάτωμα θορύβου ±0.04) — γράφονται
ως υπερασπίσιμες αποφάσεις με βάση σύγκλιση ενδείξεων, όχι ως αποδείξεις. Η διάκριση
ένδειξης/απόδειξης είναι ισχυρότερη από ισχυρισμό βεβαιότητας.*
