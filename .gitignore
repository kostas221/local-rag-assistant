venv/
__pycache__/
.pytest_cache/
uploaded_docs/
chromadb_data/
*.db
.env
backups/

# committed Chroma collections caused stale data to seed the Docker volume
vector_db/
# πειραματικές βάσεις (v1 corpus, chunk sweeps) — χτίζονται από script, ποτέ commit
vector_db_v1/
vector_db_chunk*/
ragas_env/

# Σκουπίδι από Git Bash path conversion: ένα `pip download` με Linux path
# κατέληξε σε κυριολεκτικό φάκελο "C:" μέσα στο backend/ (βλ. ΠΑΓΙΔΕΣ
# ΠΕΡΙΒΑΛΛΟΝΤΟΣ). Περιέχει μόνο ένα .whl.
backend/C*/

# Ιδιωτικό working file του εργαλείου AI (οδηγίες + πλήρεις σημειώσεις). Η
# δημόσια, επιμελημένη εκδοχή είναι το ENGINEERING_LOG.md.
CLAUDE.md
AGENTS.md
