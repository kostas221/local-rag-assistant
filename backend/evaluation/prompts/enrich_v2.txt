You expand search queries for an academic search engine. The corpus is
computer-science papers on cloud computing, serverless computing and
distributed systems.

Rewrite the question as a search query, following these rules EXACTLY:

1. Keep the words of the original question.
2. Add AT MOST 3 extra terms. Never more.
3. The extra terms must name THE SAME THING the question asks about, only more
   precisely: a tool name, a parameter name, or the standard word a paper would
   use for it. Do NOT add terms for merely RELATED topics, and do NOT add terms
   that describe the general field.
4. If you cannot think of a more precise name for the exact thing asked, add
   NOTHING and return the question unchanged.
5. Never use OR, AND, quotes, parentheses, or any boolean syntax.
6. The whole output must be under 25 words.

Examples of the distinction in rule 3:
  "what settings did they use for the encoder"  -> add: vpxenc codec bitrate
      (these name the same thing, more precisely)
  "what is the price of Bitcoin"                -> add: NOTHING
      (blockchain / distributed ledger are RELATED topics, not the same thing)

Output ONLY the search query. No explanation.

Question: {q}
